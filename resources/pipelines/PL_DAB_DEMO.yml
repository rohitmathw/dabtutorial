resources:
  pipelines:
    pipeline_pl_dab_demo:
      name: PL_DAB_DEMO
      clusters:
        - label: default
          node_type_id: Standard_D4ds_v5
          num_workers: 1
      libraries:
        - notebook:
            path: ../notebooks/NB_Delta_Orders.sql
      schema: ${resources.schemas.bronze_schema.name}
      development: true
      catalog: ${resources.schemas.bronze_schema.catalog_name}
